# Third-party web assets

The generated `internal/oauthserver/assets/material-web.js` bundle contains
the selected components from `@material/web` 2.5.0 and its runtime
dependencies. `@material/web` is released by Google under the Apache License
2.0; the dependency package includes the complete license text. The generated
bundle retains its inline license comments.

The exact dependency and integrity versions are recorded in `package-lock.json`.
